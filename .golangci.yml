version: "2"

linters:
  default: none
  enable:
    - cyclop
    - govet
    - ineffassign
    - unused
  settings:
    cyclop:
      max-complexity: 60
  exclusions:
    paths:
      - ^examples/
      - ^runs/
